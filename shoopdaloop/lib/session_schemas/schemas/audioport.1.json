{
    "type": "object",
    "properties": {
        "schema":         { "const": "audioport.1" },
        "id":             { "type": "string"  },
        "name_parts":     { "type": "array", "items": { "type": "string" } },
        "direction":      { "enum": ["input", "output"] },
        "volume":         { "type": "number" },
        "muted":          { "type": "boolean" },
        "passthrough_to": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["schema", "id", "name_parts", "direction", "volume", "muted", "passthrough_to"]
}