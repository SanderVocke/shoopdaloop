{
    "type": "object",
    "properties": {
        "schema":        { "const": "script.1" },
        "id":            { "type": "string"  },
        "name":          { "type": "string" },
        "length_cycles": { "type": "integer" },
        "elements": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "start_cycle": { "type": "integer" },
                    "elem": { "properties": { "schema": { "const": "script_elem.1", "$comment": "see script_elem.1 schema" }} }
                }
            }
        }
    },
    "required": ["schema", "id", "name", "length_cycles", "elements"]
}