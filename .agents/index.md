Project-specific information (read them on a need-to-know basis):

- `build.md`: how to build
- `test.md`: the test suites and how to run them
- `troubleshooting.md`: common pitfalls with workarounds. read when having runtime issues running the tests or app, which seem unrelated to the task at hand.
- `ci-repro.md`: techniques for reproducing CI resource-contention failures locally