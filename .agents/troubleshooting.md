# Qt QPA platform

If you get a Qt initialization error because the default QPA Platform is not available/working, try with `QT_QPA_PLATFORM=offscreen`

# Sandboxing

You are typically running inside a bubblewrap sandbox. If this limits your ability to complete the task, report this and the user will fix.