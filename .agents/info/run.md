# Run the application

Use the execution environment selected by the build guidance before running these commands.

Native development:

```sh
cargo run -p shoopdaloop
```

The first launch uses the dummy/offline driver. Configure JACK or CPAL+midir under **Settings → Audio**.

For browser development, choose the run workflow appropriate to the available tools and intended target. Use HTTPS or localhost for portable browser audio/MIDI permission behavior.
