name: Qoverage
description: Run Qoverage.

inputs:
  command:
    type: string

runs:
  using: "composite"
  steps:
  - name: Run Qoverage via Docker
    uses: addnab/docker-run-action@v3
    with:
      image: 
      options: ${{ inputs.command }} sandervocke/qoverage:latest
