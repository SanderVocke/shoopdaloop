name: App run test
description: Integration test which runs the full ShoopDaLoop app.

inputs:
  variant_name:
    required: true
    type: string
  root_path:
    type: string
    required: true

runs:
  using: "composite"
  steps:
  - name: run app
    if: always()
    shell: wrap-shell {0}
    run: >
        ${{ env.RUN_CMD_PREFIX }} ./scripts/run_with_timeout.sh 900 ${{ env.COMMAND_SHOOPDALOOP }} --quit-after=40 --backend=dummy --monkey-tester


