name: post_build_macos
description: Steps after building wheel for MacOS

inputs:
  wheel:
    required: true
    type: string

runs:
  using: "composite"
  steps:
  - name: Dummy
    shell: bash
    run: |
      echo "Dummy step"