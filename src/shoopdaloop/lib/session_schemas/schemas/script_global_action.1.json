{
    "type": "object",
    "properties": {
        "schema":       { "const": "script_global_action.1" },
        "action": {
            "enum": [
                "stopAll"
            ]
        },
        "delay_cycles": { "type": "integer" },
        "args":         { "type": "object" }
    },
    "required": ["schema", "action", "delay_cycles"]
}