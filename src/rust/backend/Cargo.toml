[package]
name = "backend"
edition = "2021"

[lib]

[[bin]]
name = "print_backend_out_dir"
path = "src/bin/print_backend_out_dir.rs"

[dependencies]

[build-dependencies]
cmake = "0.1"
anyhow = "1.0"
glob = "0.3"

[features]
# Dummy build to build dependencies only
prebuild = []