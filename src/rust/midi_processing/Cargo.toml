[package]
name = "midi_processing"
edition = "2021"

[lib]

[dependencies]
common = { path = "../common" }
shoop_engine = { path = "../shoop_engine" }

[lints]
workspace = true

[dev-dependencies]
assert2 = { workspace = true }

[features]
# Dummy build to build dependencies only
prebuild = ["common/prebuild", "shoop_engine/prebuild"]