[package]
name = "backend_rust"
links = "backend_rust"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
cxx = { workspace = true }
log = { workspace = true }
parking_lot = "0.12"
refilling_pool = { path = "../refilling_pool" }

[build-dependencies]
cxx-build = { workspace = true }

[features]
prebuild = []
