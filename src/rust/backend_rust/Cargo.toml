[package]
name = "backend_rust"
links = "backend_rust"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
cxx = { workspace = true }
log = { workspace = true }
crossbeam-channel = "0.5"

[build-dependencies]
cxx-build = { workspace = true }

[features]
prebuild = []
