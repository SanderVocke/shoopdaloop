# Composite-loop feature parity inventory

## Scope and status

This is the Stage 0 inventory of user-visible and persistence behavior at commit `06266290769186be70d4bad9def74723ff9fd315`. The original **Not migrated** labels are retained as historical baseline status. Stages 1–3 added the core-engine compiled plan, authoritative sample timeline, command boundary, replacement, reclamation, and snapshots. Stage 4 now provides an application registry and a frontend QObject adapter that translates the established QML schedule to stable engine identities, submits it, routes controls, and mirrors snapshots. The stage-specific tables below distinguish that implemented path from parity that is still unverified; engine-only evidence still does not count as end-to-end application parity.

Evidence abbreviations:

- **QML:** application/integration test, primarily `src/qml/test/tst_CompositeLoop_running.qml`.
- **Engine:** future engine-only test required by the migration.
- **Manual:** future user validation; never a substitute for automatable behavior.

## Parity matrix

| ID | Feature and current behavior | Current code/test evidence | Current timing/order dependency or gap | Migration status | Assigned verification |
|---|---|---|---|---|---|
| F01 | **Trigger-only model and creation.** Any non-sync empty slot can become a composite with no direct channels. An empty basic loop can be converted; a non-empty or sync loop cannot. Clearing a composite destroys the adapter. New tracks can be created as trigger-only. | `LoopWidget.qml:create_composite_loop`, `clear`; `NewTrackDialog.qml` trigger-only type; QML `tst_TwoLoops.qml::test_switch_between_backend_and_composite`, `test_set_gains_then_make_backend_loop`; `tst_CompositeLoop_running.qml::test_make_scheduled_loop_composite`. | Creation, conversion, and destruction use QML object lifetime and registry pointers. | Not migrated | Engine lifecycle/identity tests; QML conversion and trigger-only creation tests; Manual context-menu flow. |
| F02 | **Quick composition authoring.** Alt-click appends the clicked loop to the first timeline; Ctrl+Alt adds it on a new parallel timeline. Repeated clicks add repeated references. The global record-cycle value is copied as an optional forced duration. | `LoopWidget.qml` click handler and `CompositeLoop.qml:add_loop`; user docs `usage.loopcontrols.rst` “Composite Loops”. | QML event handling and mutable JS arrays prepare the schedule. | Not migrated | Engine compiler tests for resulting descriptors; QML mouse/modifier authoring test; Manual discoverability check. |
| F03 | **Advanced editor.** The details pane/editor displays parallel timelines and supports zoom, add/drag, sequential linking, unlinking, new parallel timelines, delay changes, duplicate/delete, forced cycle count, per-entry script mode, and regular/script conversion. | `DetailsPane.qml`; `EditCompositeLoop.qml` functions `push_playlists`, `add_connected_elems_and_push`, `unlink_and_push`, `duplicate_elem_and_push`, `delete_elem_and_push`, `force_elem_n_cycles_and_push`, `change_mode_and_push`, `to_regular`, `to_script`. | Editor is currently frontend-only and has little direct automated coverage; `LoopDetailsWindow.qml` contains an obsolete “not yet supported” placeholder while `DetailsPane.qml` loads the editor. | Not migrated | Engine plan-descriptor tests; dedicated QML editor operation/save test; Manual drag/drop and layout check. |
| F04 | **Sequential elements.** Sections in a playlist run sequentially and composite progress spans their total duration. | `CompositeLoop.qml:recalculate_schedule`; QML `test_sequential`; user docs playback/recording sections. | Schedule is calculated in QML and actions are emitted after polled sync-cycle detection. | Not migrated | Engine schedule/state-machine exact-trace test; retain QML `test_sequential`; Manual audio sequence. |
| F05 | **Parallel timelines and parallel entries.** Playlists are independent parallel timelines; entries inside one section are also parallel. Section/timeline length is their maximum end. | `CompositeLoop.qml:recalculate_schedule`; `EditCompositeLoop.qml`; QML `test_parallel_playlist`; session fixture has two playlists in `tst_Session_save_load.qml`. | JS object/set construction and later Rust hash containers erase deterministic source order. | Not migrated | Engine compiler and coincident-action tests; retain QML parallel test; Manual mixed-length parallel scene. |
| F06 | **Delays and repeated child references.** Per-entry delays create idle gaps. The same child may occur repeatedly; coincident end/start currently gets removed to avoid a restart. | `CompositeLoop.qml` playlist format and end/start removal; QML `test_sequential`, `test_parallel_playlist`; docs explicitly permit repeated clicks. | Current end/start removal also erases a coincident explicit mode change. New normalization is defined in `SEMANTICS.md`. | Contract only for normalization; runtime not migrated | Engine delay/repeat/continuation/mode-change tests; QML repeat and gap test; Manual no-glitch repeat. |
| F07 | **Explicit and length-derived duration.** Positive `n_cycles` overrides duration. Otherwise QML uses child `n_cycles` when positive and not recording, else one cycle; child `n_cycles` is `ceil(length / sync_length)`. | `CompositeLoop.qml:recalculate_schedule`; `LoopWidget.qml:n_cycles`; QML `test_parallel_playlist`, `test_grab_ringbuffer_synced_fixed_length`, sequential tests with 200/300-sample children. | Child length/mode is observed by QML; malformed zero/negative values are mostly coerced instead of rejected. | Not migrated | Engine compiler validation and ceil-duration tables; QML explicit/default length tests; Manual editor duration display. |
| F08 | **Empty, ignored, or unavailable children.** User docs say empty children are skipped. Current schedule reserves at least one cycle; playback of a zero-length primitive has no useful output, while recording can fill it. Missing registry objects are skipped during QML compilation and may be retried when created. | `CompositeLoop.qml` `Math.max(1, ...)`, `loops_ignored`, missing-object branches; docs playback/recording; QML recording/grab tests begin with empty children. | Current `loops_ignored` is effectively unreachable for ordinary entries because duration is clamped to at least one. Missing references silently alter length. Target semantics now reject missing references and define mode-sensitive empty behavior. | Contract documented; runtime not migrated | Engine empty playback/recording/missing-reference tests; QML empty child parity test; Manual empty-slot recording. |
| F09 | **Regular composites inherit modes.** Entries without explicit mode inherit Playing, Recording, PlayingDryThroughWet, RecordingDryIntoWet, or Stopped from the composite. Regular playback cycles. | `CompositeLoop.qml:ensure_script_or_regular`; backend `do_triggers_impl`; editor help; QML `test_sequential`, `test_play_after_record`; UI exposes ordinary mode controls. | Inheritance occurs in the update-thread adapter and child commands then cross to the engine. | Not migrated | Engine state-machine tables for every mode; QML regular play/record/play-dry/re-record tests; Manual dry/wet track. |
| F10 | **Script composites.** If entries have modes the plan is a script; all entries are made explicit by the current QML normalization. Scripts execute explicit modes and stop after one pass. Editor conversion defaults unspecified entries to Playing. Grab is hidden for scripts. | `CompositeLoop.qml:ensure_script_or_regular`; backend script completion; `EditCompositeLoop.qml:to_script`; `LoopWidget.qml` grab visibility; QML `test_script`. | Mixed plans are filled using the first mode encountered; target contract instead rejects mixed explicit/implicit plans. One-shot completion is update-thread driven. | Not migrated | Engine explicit-mode, mixed-plan rejection, and one-shot tests; retain QML script test and add grab-hidden test; Manual script editor. |
| F11 | **Playback and cycling.** Regular composites repeatedly wrap, increment a composite cycle number, emit `cycled`, and restart iteration zero; contiguous child actions should settle without an idle sample. | Backend `handle_sync_loop_trigger`, `do_triggers_impl`; QML `test_sequential`, nesting tests. | Cycle detection originates from frontend polling of basic-loop positions and may miss exact/multiple wraps. `cycled` is a Qt signal. | Not migrated | Engine exact-sample and multi-wrap tests; QML cycle/reporting test; Manual long playback. |
| F12 | **Stop, cancellation, retrigger.** A stop can be immediate or synchronized/countdown-based. It cancels tracked running children. A new transition replaces pending state according to backend transition behavior. | Backend `transition_regular`, `cancel_all`, `handle_transition`; `LoopWidget.qml:on_stop_clicked`; QML `test_countdown` plus broad loop-control tests. | `running_loops` comes from a hash set and can lag; stop versus due schedule action was connection-order dependent. Contract now makes source stop suppress the due action. | Contract only for ordering; runtime not migrated | Engine same-boundary stop/start/cancel/retrigger tests; QML composite stop and cancellation tests; Manual near-boundary controls. |
| F13 | **Composite recording and first occurrence only.** Regular recording inherits Recording. Repeated references record only the first occurrence in a pass; later occurrences stop/idle. | Backend `do_triggers_impl` scans earlier starts; docs “Recording”; current QML `test_play_after_record` covers recording but not a repeated child. | Earlier-start scan and trigger timing live outside RT; no direct repeated-record regression test. | Not migrated | Engine first-occurrence/repeat test; new QML repeated-record test; Manual repeated child recording. |
| F14 | **Play after record on/off.** At regular-record pass end the composite either begins playback at iteration zero or stops, and children follow. The global toggle is forwarded to each composite adapter. | Backend pass-end branch and `set_play_after_record`; `CompositeLoop.qml` binding; QML `test_play_after_record`, `test_transition_with_instant_sync_play_after_record`, `...no_play_after_record`; docs. | Toggle delivery is queued Qt state and can race the pass end. Target behavior uses the value accepted in RT before the boundary. | Not migrated | Engine accepted-command cutoff and both outcomes; retain both QML variants; Manual toggle near pass end. |
| F15 | **Delayed transitions/countdown.** A composite exposes pending mode and remaining sync boundaries. `delay=n` skips `n` sync triggers and starts on the next one; delay zero arms the next eligible boundary. | Backend `transition_regular`, `handle_sync_loop_trigger`; QML `test_countdown`; displayed timer in `LoopWidget.qml`. | Countdown decrements from polled `cycled` signals rather than engine sample events. | Contract only for acceptance/counting; runtime not migrated | Engine countdown boundary tables and cancellation; retain QML `test_countdown`; Manual timer display. |
| F16 | **Immediate synchronization/seek.** A command may immediately place the composite at an arbitrary iteration, reconstruct each child's last mode/start, derive child cycle offset, and schedule next-boundary transitions. First, middle, last, changed iteration, recording, and play-after-record outcomes exist. | Backend `transition_with_immediate_sync`, `list_transitions`; QML six `test_transition_with_instant_sync_*` tests. | Replays schedule into allocating `HashMap`s on the update thread and applies targets in hash order; divide-by-zero/invalid iteration handling is weak. | Contract only for bounds/timing; runtime not migrated | Engine precomputed seek tests for first/middle/last/change/record and no allocation; retain all six QML tests; Manual seeks while audio runs. |
| F17 | **Composite nesting in regular/script combinations.** A regular can trigger a script and keep it cycling; a script can trigger a regular for one pass. Nested starts propagate to primitive children. | Backend `dependent_will_handle_sync_loop_cycle`; QML `test_composite_triggers_script`, `test_script_triggers_composite`. | Recursion is Qt direct invocation. Child notification order comes from `HashSet`; propagation is not sample-authoritative. Existing tests cover two combinations at one level only. | Contract only for same-sample/DAG order; runtime not migrated | Engine all four kind combinations and several levels at one sample; retain QML nesting tests and add deeper save/load; Manual scene/song nesting. |
| F18 | **Coincident/conflicting targets.** Parallel composites may target the same primitive or composite. Current behavior has no deliberate policy beyond call/hash order. | Hash collections in schedule/backend; no current focused test. | Explicit accidental behavior; target contract gives direct > script > regular > natural, stable-ID tie-break. | Contract only | Engine conflict matrix and shuffled insertion-order test; QML end-to-end conflict trace; Manual not required. |
| F19 | **State and running-child reporting.** UI observes mode, next mode/countdown, iteration, sync length/position, total length/position, cycle number, and a list/set of running children. Composite position is iteration times sync length plus sync position while running. | Backend bridge properties; backend `update_length`, `update_position`; GUI queued property mirroring; `CompositeLoop.qml` aliases; QML state assertions throughout `tst_CompositeLoop_running.qml`. | All state is adapter-owned; running-child list/hash order is unstable and GUI observations are queued. | Not migrated | Engine snapshot field/order tests; QML displayed property and delayed-observation test; Manual progress indicator. |
| F20 | **Visual identity and child highlighting.** Regular composites are pink/list icons, scripts green/script icons; selecting one composite highlights its children. Empty/connected/countdown/mode visuals remain available. | `LoopWidget.qml` `StatusRect`, `LoopStateIcon`, `is_in_selected_composite_loop`; docs describe pink selection. | Visual behavior depends on QML registry sets; no direct test. | Not migrated (UI adapter remains valid but must consume snapshots) | QML visual/property test; Manual appearance and selection; no engine test beyond snapshot children. |
| F21 | **Grab rejects an empty/undefined sync source.** Composite grab has no effect when sync length is zero. Only regular composites expose grab. | Backend `adopt_ringbuffers`; QML `test_grab_ringbuffer_sync_empty`; UI visibility; docs. | Warning/action occurs on update thread and grabs invoke children directly. | Not migrated | Engine validation/transaction test; retain QML empty-sync test; Manual button availability. |
| F22 | **Synchronized composite grab.** The complete composite duration is mapped so its last cycle ends at the last completed sync cycle. Each child's first Recording range is grabbed; explicit durations are honored. Final outcome is stopped or immediate playback based on play-after-record. | Backend `adopt_ringbuffers`; QML `test_grab_ringbuffer_synced_then_stop`, `...fixed_length`, `...then_play`; docs. | Builds maps/vectors and makes sequential child calls outside RT; no atomic engine transaction. | Not migrated | Engine prepared-grab transaction/sample-marker tests for default/fixed and stop/play; retain three QML tests; Manual representative input. |
| F23 | **Unsynchronized composite grab.** The current sync cycle maps to the final composite cycle; completed portions are adopted and the final child records the remainder, then stops or starts composite playback. | QML `test_grab_ringbuffer_unsynced_then_stop`, `...then_play`; backend reverse-start adjustment; docs. | Frontend coordinates grab and follow-up transition through separate calls, so transaction timing is not authoritative. | Not migrated | Engine exact marker and atomic-outcome tests; retain both QML tests; Manual seamless grab. |
| F24 | **Schedule recalculation and lifecycle.** Schedules react to playlist edits, child creation/registry discovery, backend initialization, conversion to composite, child cycle-count/length changes, and sync length. Schedule is frozen during recording to avoid length-feedback changes. Missing/deleted children cease to resolve. | `CompositeLoop.qml` registry `Connections`, `Mapper`, `schedule_frozen`; GUI `update_backend_schedule`; QML `test_make_scheduled_loop_composite`. | QML signals determine when a schedule changes. Deletion and multilevel conversion are weakly tested; a running schedule can change at arbitrary update-thread time. | Not migrated; activation contract defined | Engine version/stale identity and stopped/pending/running replacement tests; QML create/delete/convert/length tests; Manual edits in all states. |
| F25 | **Circular-reference rejection.** Self-reference and a direct two-composite cycle clear/reject the newly assigned playlist while preserving the previously valid other playlist. | `CompositeLoop.qml:onPlaylists_inChanged`; QML `test_circular_composites`, `test_circular_composite_self`. | Current check is frontend registry traversal and does not prove rejection of cycles longer than two or candidate topology races. | Contract only with transitive cycle utility | Engine self/two/deep/candidate-topology cycle tests; retain QML tests and add three-level case; Manual not required. |
| F26 | **Session persistence and compatibility.** `loop.1` stores `composition.playlists`; each entry stores `delay`, `loop_id`, optional integer `mode`, optional integer `n_cycles`. References resolve after loop widgets register. Composite loops have composition instead of channel data. Save/reload and cross-session load preserve direct and empty composites. | `src/session_schemas/schemas/loop.1.json`; `LoopWidget.qml:actual_session_descriptor`, `init`; `CompositeLoop.qml:actual_composition_descriptor`; QML `tst_Session_save_load.qml` setup/assertions. | Schema permits channels and composition together and does not constrain integer ranges; QML rejects the combination only during data loading. Existing save/load test does not execute a nested script after reload. | Not migrated | Engine/frontend descriptor validation; QML existing compatibility plus nested regular/script execution after reload; Manual existing-session round trip. |
| F27 | **Selected/group/solo/targeted controls.** Composite transitions participate in selected-loop group calls and solo-in-track stopping. Targeting can supply transition delay/alignment like a basic loop. Composite grab intentionally ignores targeted-loop duration according to docs, although generic `LoopWidget` branching needs parity tests. | `LoopWidget.qml:transition`, `transition_solo_in_track`, `delay_for_targeted`, click handlers; docs selection/targeting and composite grab exceptions; Lua controls use the same widget API. | Mixed basic/composite group transitions split into one backend batch plus per-composite Qt calls, so same-boundary atomicity is absent. | Not migrated | Engine unified mixed-target command test; QML selected/solo/targeted composite tests; Manual keyboard/MIDI near boundaries. |
| F28 | **Clear/unload and child cleanup.** Clear empties a composition and removes the composite object; backend `clear` resets runtime/public fields. Stop/cancel cleans tracked children. | `CompositeLoop.qml:clear`; `LoopWidget.qml:clear`, `unload`; backend `clear`, `cancel_all`; QML suite `clear()` between cases. | QObject `deleteLater`/queued signals and weak pointers govern teardown; stale queued events are possible. | Not migrated | Engine tombstone/generation/teardown and no-RT-destruction tests; QML session replacement/clear test; Manual not required. |
| F29 | **Lua/control API composition.** `shoop_control.loop_compose_add_to_end` creates or appends to a composition, including parallel choice; ordinary trigger/grab APIs then operate on it. | `qobj_session_control_handler.rs` composition API; QML `tst_LuaEngine_SessionControlHandler.qml::test_loop_compose_add_to_end`, `test_loop_trigger_grab`. | Lua call ultimately mutates QML playlists; acceptance and validation errors are not engine statuses. | Not migrated | QML Lua create/configure/error/execute tests; engine compiler reached through frontend API; Manual controller script. |
| F30 | **GUI/update-thread stall tolerance.** Once configured, current composites continue while the QML GUI thread or blocking file I/O stalls because the adapter lives on the separate engine update thread. | GUI moves backend object in `qobj_composite_loop_gui.rs`; QML `test_ui_frozen`, `test_fileio_frozen`. | This is not RT authority: a stalled update thread, delayed polling, or missed wrap still changes timing. | Not migrated | Engine trace test that stalls both frontend and update processing while audio continues; retain QML stall tests with engine snapshots; Manual heavy GUI load. |
| F31 | **Documentation and discoverability behavior.** User docs describe scenes/sequences/scripts, playback, recording-first-occurrence, and composite grab. Context menu creates a composite and the details pane loads an editor. | `docs/source/concept.rst`, `docs/source/usage.loopcontrols.rst`; `LoopWidget.qml` context menu; `DetailsPane.qml`; `EditCompositeLoop.qml`. | Docs incorrectly say advanced editing is unimplemented and contain minor contradictions with empty-child scheduling; permanent docs need reconciliation after migration. | Inventory complete; docs migration pending later stages | Documentation review; QML smoke tests for menu/editor; Manual workflow. |

## Stage 1 engine coverage update

The Stage 0 matrix above remains the baseline inventory. Stage 1 adds the immutable engine plan and pure state machine described in [ARCHITECTURE.md](ARCHITECTURE.md). “Implemented (pure)” means the non-audio, non-frontend semantics are executable in `shoop_engine`; it does **not** mean the application has migrated. “Deferred” identifies a row whose essential verification requires a later routing, command, frontend, or QML stage.

| ID | Stage 1 applicability and evidence | Stage 1 status |
|---|---|---|
| F01 | Stable basic/composite identities, generation rejection, and pure clear are tested; `Session` creation/conversion remains later. | Partial |
| F02 | Nested timeline/section/entry descriptors compile independent of authoring order; mouse authoring remains QML. | Implemented (pure compiler) |
| F03 | The descriptor supports parallel/sequential, delay, duplicate, cycle override, and modes; editor gestures/layout remain QML. | Implemented compiler surface; UI deferred |
| F04 | `compiler_flattens_sequential_parallel_delayed_and_repeated_entries` and runtime boundary tests. | Implemented (pure) |
| F05 | Parallel sections/timelines, maximum lengths, canonical target/action ordering. | Implemented (pure) |
| F06 | Delay/gap metadata, repeated occurrence numbers, same-mode continuation, and explicit mode changes. | Implemented (pure) |
| F07 | Explicit/derived duration and ceil/overflow validation tests. | Implemented (pure) |
| F08 | Missing/stale rejection plus mode-sensitive empty playback/recording tests. | Implemented (pure) |
| F09 | Regular inheritance covers Playing, Recording, Replacing, and dry/wet modes. | Implemented (pure) |
| F10 | Explicit modes, mixed-plan rejection, and one-pass script completion. | Implemented (pure) |
| F11 | Regular wrap, cycle counter, contiguous no-retrigger, long-running and integer-edge tests. | Implemented (pure) |
| F12 | Immediate/delayed stop, pending replacement, cancellation, and stable child cleanup. | Implemented (pure) |
| F13 | Repeated-target recording stops after occurrence zero. | Implemented (pure) |
| F14 | Both play-after-record outcomes are tested from accepted runtime state. | Implemented (pure) |
| F15 | Countdown skips exactly `n` boundaries while the old pass advances; due transition replaces old delivery. | Implemented (pure) |
| F16 | Bounded direct seek-table lookup covers changed iteration, offsets, stops, invalid bounds, and unchanged-on-error state. | Implemented (pure) |
| F17 | All four parent/child regular/script plan-kind combinations compile to stable composite identities; recursive same-sample delivery is Stage 2. | Partial; propagation deferred |
| F18 | Same-plan conflicts are insertion-order independent and outputs are stop-before-set; cross-source resolver remains Stage 2, with precedence already pinned by Stage 0 tests. | Partial |
| F19 | Mode, pending/countdown, iteration, length, position, cycle count, counters, and stable active children are runtime getters; snapshot transport remains Stage 3. | Implemented state; transport deferred |
| F20 | Deterministic active-child identity is available for highlighting; visual rendering is QML-only. | Engine portion implemented |
| F21 | Grab-specific empty-sync transaction requires ringbuffer/audio routing. | Deferred to Stage 4 |
| F22 | Synchronized grab requires prepared marker/ringbuffer integration. | Deferred to Stage 4 |
| F23 | Unsynchronized grab requires prepared marker/ringbuffer integration. | Deferred to Stage 4 |
| F24 | Compilation is atomic; stale targets and stopped/pending/running activation decisions are tested. Queue ownership and lifecycle-trigger submission remain Stages 3–5. | Partial |
| F25 | Self and transitive candidate-topology cycles plus graph/depth capacities are tested. | Implemented (compiler) |
| F26 | Numeric engine descriptor validation is implemented; `loop.1` translation and compatibility remain frontend/QML work. | Partial |
| F27 | Unified mixed-target command batching is a callback/control-boundary concern. | Deferred to Stages 3/5 |
| F28 | Pure stop/clear cleanup and generation-safe stale handling are tested; non-RT object/plan reclamation remains Stage 3. | Partial |
| F29 | Engine compiler API exists; Lua/QML API routing to it remains Stage 5. | Deferred integration |
| F30 | Pure runtime has no frontend/update dependency, but callback authority and stall tests require Stages 2–5. | Deferred integration |
| F31 | Prototype architecture and semantics docs are current; permanent user-doc reconciliation remains Stage 5. | Stage 1 documentation complete |

Every matrix row whose Stage 1 portion is independent of audio routing and frontend integration is covered above. Engine test names and requirement-level evidence are listed in [ARCHITECTURE.md](ARCHITECTURE.md#stage-1-verification-map).

## Stage 2 engine coverage update

Stage 2 makes compiled composites authoritative inside the core `Session` sample timeline and adds bounded same-sample resolution. “Engine timing implemented” still does not mean the frontend application submits plans or observes this state; that switch remains Stages 3–5.

| ID | Stage 2 applicability and evidence | Stage 2 status |
|---|---|---|
| F01 | Session exposes stable basic identities and owns an installable composite timeline; application creation/conversion is later. | Engine timing partial |
| F02 | Authoring remains QML; accepted compiled schedules execute on the sample timeline. | Deferred UI; engine execution available |
| F03 | Editor remains QML; descriptor/plan behavior is unchanged. | Deferred UI |
| F04 | Sequential boundaries now land on authoritative source POIs and exact output samples. | Engine timing implemented |
| F05 | Parallel/coincident outputs are gathered before stable target commit. | Engine timing implemented |
| F06 | Delays/repeats execute at source boundaries; continuation remains normalized by Stage 1. | Engine timing implemented |
| F07 | Compiled durations drive sample-authoritative iteration timing. | Engine timing implemented |
| F08 | Empty/missing semantics remain Stage 1; stale basic targets are not redirected by Session identities. | Engine timing implemented for accepted plans |
| F09 | Regular inherited modes are committed before post-boundary samples. | Engine timing implemented |
| F10 | Script explicit modes and one-pass completion execute through the resolver. | Engine timing implemented |
| F11 | Multiple wraps in one callback advance every composite boundary and preserve partition-independent traces. | Engine timing implemented |
| F12 | Direct source stop suppresses coincident due delivery; child cleanup is resolved with other intents. | Engine timing implemented |
| F13 | First-occurrence recording remains enforced by the runtime at sample boundaries. | Engine timing implemented |
| F14 | Runtime pass-end decisions now commit at authoritative boundaries; accepted toggle transport remains Stage 3. | Engine timing implemented; command transport deferred |
| F15 | Countdown advances from source trigger events rather than frontend polling. | Engine timing implemented |
| F16 | Immediate seek can enter through accepted direct boundary control; full application command API remains later. | Engine mechanism implemented |
| F17 | Several composite levels propagate iteration-zero actions at one sample in stable DAG order. | Engine timing implemented |
| F18 | Direct > script > regular > natural precedence and stable same-class tie-breaks execute in the resolver. | Engine timing implemented |
| F19 | Runtime fields remain authoritative; snapshot transport is Stage 3. | State implemented; transport deferred |
| F20 | Stable active-child identity remains available; visual integration is later. | Deferred UI |
| F21 | Composite grab is outside Stage 2. | Deferred Stage 4 |
| F22 | Synchronized grab is outside Stage 2. | Deferred Stage 4 |
| F23 | Unsynchronized grab is outside Stage 2. | Deferred Stage 4 |
| F24 | Prepared timelines validate topology/capacity; plan queue and replacement ownership remain Stage 3. | Timing implemented; lifecycle partial |
| F25 | Installed target and sync-source edges are topologically sorted; cycles/depth are rejected before processing. | Engine timing implemented |
| F26 | Persistence translation is outside Stage 2. | Deferred frontend |
| F27 | Resolver accepts direct intents at exact samples, but unified application command batching is Stage 3/5. | Engine mechanism partial |
| F28 | Session tombstones prevent stale basic identity redirection; non-RT reclamation remains Stage 3. | Engine timing partial |
| F29 | Lua/QML routing remains later. | Deferred integration |
| F30 | Configured engine composites continue solely from callback sample processing; frontend/update-stall end-to-end proof remains Stage 5/6. | Engine timing implemented; app proof deferred |
| F31 | Prototype architecture, RT-safety, semantics, and parity documentation now include Stage 2. | Stage 2 documentation complete |

Stage 2 timing evidence is mapped in [ARCHITECTURE.md](ARCHITECTURE.md#stage-2-verification-map) and [RT_SAFETY.md](RT_SAFETY.md#stage-2-verification-evidence). No row is labeled application-migrated merely because the engine path exists.

## Stage 3 partial engine coverage update

This table records the implemented control/publication slice; the Stage 4 update below records the application adapter separately.

| ID | Stage 3 evidence | Status |
|---|---|---|
| F12 | Direct stop/start uses the same accepted target-action queue; stopping before deferred activation cleans children and activates the candidate stopped. | Engine control/replacement implemented |
| F14 | Play-after-record is accepted in callback queue order and published from RT state. | Engine command/state implemented |
| F15 | Dedicated synchronized transition commands arm the RT countdown; snapshots publish pending mode and remaining boundaries. | Engine command/state implemented |
| F16 | Dedicated immediate transition validates exact iteration before acceptance and executes without replay. | Engine command/state implemented |
| F19 | Reusable snapshots publish every required composite field, deterministic active children, counters/fault, version, and rolling trace. | Engine transport implemented; adapter mirrors primary state |
| F24 | Exact primitive topology and monotonic versions reject stale/out-of-order preparation. Stopped/pending activation and same-dependency-topology running activation are atomic; running dependency-topology changes remain open. | Lifecycle mostly implemented in engine |
| F27 | Basic and composite targets can share the direct boundary-control path and resolver. Application group batching is open. | Engine mechanism implemented; app batching open |
| F28 | Executed commands and displaced/rejected timelines return for non-RT destruction; frontend clear removes the registry node after deterministic stop/cleanup. | Ownership/application API implemented |
| F30 | Rolling traces remain observable after snapshot polling stalls while callback authority continues. | Engine proof implemented; QML stall proof open |

Evidence is listed under the Stage 3 partial verification map in [ARCHITECTURE.md](ARCHITECTURE.md#stage-3-partial-verification-map).

## Stage 4 application adapter update

- `BackendSession` allocates stable, generation-bearing composite identities and exposes configuration, transition, immediate seek, record option, removal, and state APIs.
- A transactional registry compiles every configured composite against one candidate catalog and dependency graph; installation succeeds before registry commit, and cycle rejection is covered by `application_composite_registry_rejects_a_cycle_transactionally`.
- `CompositeLoopBackend` resolves QML backend wrappers to basic/composite `LoopIdentity` values, translates existing boundary schedules to descriptors, obtains the published primitive topology, and submits prepared timelines off RT.
- Once engine initialized, transitions and play-after-record use engine commands, wrap/recursive QObject trigger handling is disabled, and `update` mirrors engine mode, pending state, iteration, cycle, position, length, active children, and record state.
- QML playlist editing and `loop.1` persistence remain structurally unchanged. The existing 24-case composite suite passes in full, including nested regular/script execution, immediate seeks, GUI/file-I/O stalls, circular references, conversion, recording, play-after-record, and every current grab variant. The complete QML suite passes 188/189 with only the host's missing CPAL playback port. Dedicated nested save/load, deletion/session-replacement, acceptance-error, and manual gates remain open, and the guarded legacy scheduler is still present in source.

## Current implementation map

1. `CompositeLoop.qml` owns persisted playlists and still derives the established JS boundary schedule.
2. `composite_loop_schedule.rs` serializes that schedule through `QVariant`; this is now configuration input, not callback timing authority.
3. `qobj_composite_loop_gui.rs` replaces QML objects with weak backend-wrapper pointers and sends configuration by queued Qt connection.
4. `qobj_composite_loop_backend.rs` resolves those pointers to stable engine identities, submits plans and commands, and mirrors snapshots. Its legacy scheduler is bypassed whenever an engine composite handle exists.
5. `qobj_loop_backend.rs` continues to expose primitive snapshots and compatibility `cycled` signals, but those signals no longer advance engine-backed composites.
6. `LoopWidget.qml`, the editor, state registries, Lua API, and session serializer retain the application surface.
7. The engine path installs versioned compiled composites, splits at authoritative POIs, propagates nested actions, resolves conflicts, commits primitive modes at exact samples, and publishes rolling state/trace snapshots; the frontend adapter now invokes this path.

## Accidental timing and ordering dependencies: migration status

These were observable baseline risks, not desired parity:

- **Polling authority (replaced for engine-backed composites):** `qobj_loop_backend.rs:update` still emits a compatibility wrap signal, but engine composite advancement comes from callback source events and does not consume it.
- **Qt delivery (configuration/observation only):** GUI-to-wrapper configuration and wrapper-to-GUI observation remain queued. Accepted controls and all runtime boundaries are ordered by the engine command/sample timeline rather than Qt delivery during execution.
- **Recursive QObject propagation (bypassed):** engine-backed `handle_sync_loop_trigger` returns immediately; nesting uses the bounded engine resolver.
- **Hash traversal (not timing authority):** the compatibility schedule contains hash collections, but the off-thread engine compiler canonicalizes stable identities/actions before installation. Remaining displayed-list/editor order is a parity/UI concern.
- **JS object/set traversal:** QML schedule assembly uses JS `Set`, object keys, and object-as-key mode lookup. This is not an engine-stable action order.
- **Signal-derived schedule updates:** child `n_cycles`, sync length, registry contents, backend initialization, and conversion trigger QML recalculation. The instant a running schedule changes depends on GUI signal processing.
- **Limited cycle validation (replaced at acceptance):** QML may still provide early feedback, while the transactional engine compiler rejects self/transitive cycles and topology races before registry commit.
- **Split mixed transitions:** basic loops transition in one engine backend call while composites transition individually on the update thread. A selected mixed group has no single sample transaction.
- **Queued control values:** play-after-record, sync mode, kind, schedule, and transitions are separate queued messages, so their relative arrival at a terminal boundary can vary.
- **State as pointers (adapter boundary only):** weak Qt pointers are resolved off RT to slot/generation/kind identities before compilation; callback state never uses object addresses.

None of these accidental behaviors is a parity requirement. Their deterministic replacements are defined in [SEMANTICS.md](SEMANTICS.md).

## Stage 0 coverage conclusion

Every behavior found in QML, frontend Rust, the `loop.1` schema, user documentation, and composite-focused tests maps to F01–F31. Every row assigns an engine test, QML test, manual check, or a justified combination. No row is represented as engine-migrated merely because the existing frontend test passes.
