# Built-in Lua Akai and keyboard audit plan

## Goal

Produce a static, end-to-end audit of the bundled controller scripts:

- `src/lua/builtins/akai_apc_mini_mk1.lua`
- `src/lua/builtins/keyboard.lua`

The completed audit must show, for every advertised binding and every significant body-only behavior, whether its Lua API is present and correctly used, whether the current application path is expected to work through the engine or other relevant boundary, which existing Rust tests prove that exact situation, and which Rust tests were added to close gaps. The result should separate well-tested behavior, plausible but insufficiently tested behavior, and suspicious or missing behavior so that later fixes can be prioritized.

This is an audit-and-test task, not a behavior-fix task. Correctness conclusions must come from reading the current code path, not from manually operating the application or controller.

## Scope

### In scope

- Every documentation-comment behavior at the top of both scripts, including every key, button, fader, press/release behavior, modifier, and advertised combination.
- Significant additional behavior found in the script bodies, identified as **body-only** in the inventory below.
- The Lua helper and coordinate implementations used by the scripts.
- Lua API registration, constants, argument conversion, selector handling, operation generation, callback dispatch, and MIDI handling.
- Application handling of generated operations and events, including model state, backend calls, state publication, and the engine boundary where applicable.
- Existing Rust tests at all layers, with an explicit assessment of whether they cover the API in the script's actual situation rather than only testing a lower-level primitive.
- New or extended Rust tests for every row whose situational coverage is insufficient. A newly added test may remain failing if it exposes a real gap; its failure and reason must be recorded.

### Out of scope

- Fixing Lua scripts, helper libraries, application behavior, engine behavior, or documentation discovered to be wrong.
- Changing advertised semantics or silently narrowing an audit row to match the implementation.
- Hardware testing, manually running the application, or treating observed runtime behavior as a substitute for a code-path audit.
- Broad refactors unrelated to focused Rust audit tests.

## Relevant code and likely evidence surfaces

Start with these surfaces and follow symbols further whenever the path continues:

- Lua scripts and libraries: `src/lua/builtins/`, `src/lua/lib/shoop_helpers.lua`, `src/lua/lib/shoop_coords.lua`, `src/lua/lib/shoop_midi.lua`, and `src/lua/lib/shoop_control.lua`.
- Lua host/API: `src/rust/shoop_scripting/src/control.rs`, `src/rust/shoop_scripting/src/lib.rs`, `src/rust/shoop_scripting/src/key_constants.rs`, and `src/rust/shoop_scripting/src/midi.rs`.
- Input and public contracts: `src/rust/shoop_egui/src/key_input.rs` and `src/rust/shoop_app_api/src/lib.rs`.
- Application-to-backend behavior and primary integration tests: `src/rust/shoop_app/src/lib.rs`.
- Engine implementation and tests: `src/rust/shoop_engine/src/` and `src/rust/shoop_engine/tests/`.

Existing broad integration tests such as `production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases`, `production_keyboard_plays_manual_recording_on_next_sync_cycle`, `unchanged_apc_script_drives_authoritative_state_and_bounded_led_output`, and `unchanged_apc_script_uses_native_virtual_midi_when_available` are search anchors, not automatic proof for every row. Count them only where their setup and assertions actually prove the listed behavior and relevant boundary.

## Immutable acceptance criteria

1. Every inventory row below is resolved, and a final line-by-line comparison against both script headers and bodies finds no omitted advertised binding, combination, press/release behavior, or significant body-only behavior.
2. Every row names the exact Lua/helper calls and has an API audit that establishes whether each function and constant exists, accepts the supplied argument shape, selects the intended loops/tracks, and produces the intended operation or callback behavior.
3. Every row contains a static path audit from input/callback through Lua and the host bridge into authoritative application code and on to the backend/engine boundary. For MIDI output, LEDs, selection, or other behavior with a different terminal boundary, that boundary and why no engine call is expected are stated explicitly.
4. Every row lists exact existing Rust evidence as `path::test_name` plus the relevant assertion or scenario. Generic API tests are labeled partial and are not treated as situational coverage.
5. Every row has one final verdict from the taxonomy below, with a concise evidence-based reason.
6. Every row deemed insufficiently tested has one or more focused Rust tests added or extended, and the exact `path::test_name` and pass/fail status are recorded. Failing tests are retained and described rather than weakened or mislabeled.
7. No production behavior or Lua documentation is changed as part of this audit without explicit user approval.
8. The completed document contains no unresolved table placeholders, shorthand, unnamed tests, or unsupported claims.

## Design and evidence rules

- Preserve the advertised behavior as the expected contract even when the body appears to disagree; record the disagreement as suspicious.
- Audit the production bundled script itself. A test of a rewritten Lua snippet is only lower-level evidence unless the behavior cannot reasonably be exercised through the bundled script.
- Distinguish four evidence levels when useful: Lua API conversion/operation tests, application tests with a fake backend, application tests with the dummy/real engine backend, and native host/device integration. State which level each test reaches.
- A test name alone is not evidence. Record what input it supplies and what state, operation, backend effect, MIDI output, or published result it asserts.
- Validate coordinate conventions (`-1` sync track versus zero-based regular tracks), selector cardinality, ordering, modifier precedence, press/release state, and empty-selection behavior explicitly.
- For transition operations, inspect synchronization, delay, alignment, repeat-sync, apply-N-cycles, and global-control handling all the way to the backend call; do not infer correctness merely from a changed application snapshot.
- For track controls, inspect fader conversion, dB conversion, balance range, stereo assumptions, muting, input monitoring, and `respect_auto_mute` behavior.
- For MIDI behavior, inspect host endpoint direction, regex matching, reconnect behavior, callback timing, rate limiting, message validation/classification, note/CC mapping, and output queue behavior.
- Do not use manual app or hardware execution as audit evidence. Rust tests may be run while authoring tests; record their exact result, including intentional failures that expose defects.
- If one Rust test covers several rows, cite it in each row and identify the distinct assertion for each. If one row needs several layers of proof, cite all relevant tests.
- Newly discovered significant body behavior must be appended with a stable `AK-*` or `KB-*` ID and marked **Body-only**. Do not remove an existing row because implementation evidence changes.

## Verdict taxonomy

- **PASS — expected and sufficiently tested**: the complete static path is sound and contextual Rust coverage reaches the relevant boundary.
- **WORKS-BUT-UNTESTED**: the complete static path appears sound, but existing contextual coverage is absent or partial. This verdict requires a new/extended Rust test in the final column; after adding a passing adequate test, normally promote the row to `PASS`.
- **SUSPICIOUS**: an API is missing/misused, advertised semantics disagree with the body, the application/backend path is incomplete, or another concrete defect risk exists. Add a focused regression/characterization test; it may remain failing.
- **NOT-APPLICABLE**: only for a clearly justified non-engine boundary or a behavior that cannot require a test. This does not waive the requirement to audit and test script-local or MIDI behavior where Rust can exercise it.

## Table evidence rules used

The first four columns are the complete inventory and remain stable except for evidence-backed clarification or newly appended body-only rows. The completed evidence columns use the following rules:

1. **API audit** — list every exact API/helper/constant used by the row and state `OK`, `MISMATCH`, or `MISSING` for existence, argument shape, coordinate/selector handling, return shape, and generated `ControlOperation` or callback effect. Include symbol paths, not just module names.
2. **Static app → boundary path** — write the concrete call chain from keyboard/MIDI event or callback through Lua, `shoop_scripting`, application operation handling, and backend/engine method. Name the final observable boundary. Mark breaks or semantic mismatches at the exact symbol.
3. **Existing Rust evidence** — cite `path::test_name` and summarize the scenario/assertion that covers this row. Write `None found (searched: …)` if no contextual test exists. Label lower-level-only evidence `Partial`.
4. **Verdict** — use exactly one taxonomy value and give a short reason tied to the preceding columns.
5. **New/extended Rust test(s)** — for insufficient coverage, cite every added `path::test_name`, what gap it closes, and `PASS` or `FAIL` with the failure reason. If existing coverage is sufficient, write `Not needed — <specific existing evidence>`. Do not write only `N/A`.

Audit one row at a time, but re-check shared helpers and operations whenever later evidence changes an earlier conclusion. Keep the table updated immediately after each investigation or test addition.

## Master audit table

| ID | Contract source and binding/event | Functionality that must be proved | Lua/helper path audited | API audit | Static app → boundary path | Existing Rust evidence | Verdict | New/extended Rust test(s) |
|---|---|---|---|---|---|---|---|---|
| AK-01 | Advertised mapping: grid notes `0..63`; CLIP STOP `82`; SOLO `83`; REC ARM/RECORD `84`; MUTE/GRAB `85`; SELECT `86`; blank 1/SYNC `87`; blank 2/sync loop `88`; STOP ALL CLIPS `89`; SHIFT `98`; VOLUME `68`; PAN `69`; SEND/DRY `70`; DEVICE/SET N CYCLES `71`; faders CC `48..56` | Physical controls and renamed labels route to the intended logical button, grid coordinate, regular track, or sync track. | `note_to_loop_coords`; `cc_to_fader_track`; button constants | `OK` — script-local note/CC constants and coordinate functions cover notes 0–63, special note 88, and CC 48–56; `shoop_midi.is_kind` masks the status nibble and the host delivers exact byte vectors. | `ScriptManager`/`ScriptCallbacks::advance_midi` matches host endpoints, owns connections, invokes the bundled callback, and drains validated/rate-limited bytes through `MidiControlService::send`; the host MIDI endpoint is the terminal boundary, not the audio engine. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` injects grid/button/CC values and asserts regular/sync routing; `src/rust/shoop_scripting/src/lib.rs::tests::complete_control_surface_is_installed_with_stable_constants` verifies mode constants. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-02 | Advertised startup behavior: controller appears/disappears | Automatically discover, open, connect, disconnect, and reconnect matching APC MIDI input/output endpoints. | `auto_open_device_specific_midi_control_input`; `auto_open_device_specific_midi_control_output` with `.*APC MINI MIDI.*` | `OK` — subscriptions, timers, regex compilation, MIDI byte validation, endpoint-direction inversion, rate limiting, and callback payloads are implemented in `install_subscriptions` and `ScriptCallbacks::advance_midi`. | `ScriptManager`/`ScriptCallbacks::advance_midi` matches host endpoints, owns connections, invokes the bundled callback, and drains validated/rate-limited bytes through `MidiControlService::send`; the host MIDI endpoint is the terminal boundary, not the audio engine. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts two owner-managed links, hot-unplug, reconnect, and teardown; `src/rust/shoop_scripting/src/lib.rs::tests::midi_service_autoconnects_hotplugs_delivers_exact_bytes_and_throttles_output` proves endpoint directions and hotplug; `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_uses_native_virtual_midi_when_available` covers host MIDI when available. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-03 | Advertised startup plus body detail: output connection/reconnection | Install `port.send`, schedule a delayed reset, and emit reset LEDs through the configured positive output rate limit without an unbounded burst. | output open/connected callbacks; `register_one_shot_timer_cb(1000, reset)`; `port.send`; `reset` | `OK` — subscriptions, timers, regex compilation, MIDI byte validation, endpoint-direction inversion, rate limiting, and callback payloads are implemented in `install_subscriptions` and `ScriptCallbacks::advance_midi`. | `ScriptManager`/`ScriptCallbacks::advance_midi` matches host endpoints, owns connections, invokes the bundled callback, and drains validated/rate-limited bytes through `MidiControlService::send`; the host MIDI endpoint is the terminal boundary, not the audio engine. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts delayed 67-message reset, one-message-per-millisecond pacing, reconnect reset, and bounded output; `src/rust/shoop_scripting/src/lib.rs::tests::callbacks_and_monotonic_timers_are_ordered_and_script_owned` and `src/rust/shoop_scripting/src/lib.rs::tests::positive_midi_rate_limit_never_catches_up_with_a_same_pump_burst` cover timer and queue mechanics. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-04 | Advertised: grid buttons indicate loop state | Loop events produce off for empty/stopped, yellow for nonempty/stopped, green for both playing modes, and red for both recording modes; cached colors suppress duplicates and reset can resend all regular/sync-loop colors. | `register_loop_event_cb`; `push_loop_color`; `push_all_loop_colors`; loop event fields and `LoopMode_*`; `port.send` | `MISMATCH` — callback/event fields and mode constants are correct, but `get_loop_color` checks `loop_colors[coords[2]]` instead of the nested x-row and reset has no authoritative initial-state query; reconnect/initial reset can send off for active loops. | `ApplicationModel` publishes committed loop/global events → `ScriptManager` callback dispatch → bundled LED/cache logic → validated output queue → `MidiControlService::send`; MIDI output is the terminal boundary. | `src/rust/shoop_app/src/lib.rs::tests::production_apc_led_colors_and_reconnect_reset_characterize_cached_color_bug` asserts red/green/yellow/off live colors and characterizes reconnect sending off for cached note 41; `src/rust/shoop_scripting/src/lib.rs::tests::callbacks_expose_complete_payloads_and_are_non_reentrant` validates loop payloads. | SUSPICIOUS — live updates work, but initial/reconnect reset can lose active-loop colors. | Added `src/rust/shoop_app/src/lib.rs::tests::production_apc_led_colors_and_reconnect_reset_characterize_cached_color_bug` — PASS; it proves all four colors and preserves a passing characterization of the reset-cache defect. |
| AK-05 | Advertised global controls plus body feedback | Global events and reset refresh SOLO and SYNC LEDs according to current global state, including the script's intentional SYNC LED polarity. | `register_global_event_cb`; `recheck_global_controls`; `get_solo`; `get_sync_active`; `port.send` | `OK` — subscriptions, timers, regex compilation, MIDI byte validation, endpoint-direction inversion, rate limiting, and callback payloads are implemented in `install_subscriptions` and `ScriptCallbacks::advance_midi`. Global getters also exist and return booleans. | `ApplicationModel` publishes committed loop/global events → `ScriptManager` callback dispatch → bundled LED/cache logic → validated output queue → `MidiControlService::send`; MIDI output is the terminal boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now asserts reset emits SOLO off and inverse-SYNC green for solo=false/sync=false, and later asserts momentary/permanent global state. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added exact reset and externally triggered global LED assertions. |
| AK-06 | Body-only: press/release feedback | SHIFT, SELECT, RECORD, GRAB, CLIP STOP, DRY, SET N CYCLES, VOLUME, and PAN LEDs turn on while held and off on release without corrupting held state. | `handle_noteOn`; `handle_noteOff`; `set_led_by_note`; state variables | `OK` — all listed notes have paired note-on/note-off state branches and `set_led_by_note` queues valid three-byte messages. | `ScriptManager`/`ScriptCallbacks::advance_midi` matches host endpoints, owns connections, invokes the bundled callback, and drains validated/rate-limited bytes through `MidiControlService::send`; the host MIDI endpoint is the terminal boundary, not the audio engine. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now presses/releases every listed held control and asserts green/off feedback. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added press/release LED assertions for all held controls. |
| AK-07 | Advertised: grid button with no action modifier | Perform the complete default loop action, including transition cancellation, empty-loop record/grab policy, recording→playing, stopped nonempty→playing, and other states→stopped. | `shoop_helpers.default_loop_action(coords, false)`; mode/length/next-mode getters; `get_default_recording_action`; `loop_trigger`; `loop_trigger_grab` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` loads the bundled script and asserts the row-specific loop mode; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies selector/mode conversion; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy`, `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle`, and `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_generate_trigger` prove helper branches and engine scheduling. | PASS — expected and sufficiently tested | Added `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` — PASS; closes stopped-nonempty, cancellation, and configured-grab helper branches shared by both scripts. |
| AK-08 | Advertised: DRY + loop/grid button | Use the dry variant of the default action so transitions to playback become Playing Dry Through Wet while all other default-action branches remain correct. | `shoop_helpers.default_loop_action(coords, true)` and its getter/trigger/grab chain | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` loads the bundled script and asserts the row-specific loop mode; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies selector/mode conversion; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy`, `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle`, and `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_generate_trigger` prove helper branches and engine scheduling. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added DRY default-action assertion for Playing Dry Through Wet. |
| AK-09 | Advertised: CLIP STOP + loop/grid button | Stop the clicked loop. | `loop_trigger(coords, LoopMode_Stopped)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` loads the bundled script and asserts the row-specific loop mode; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies selector/mode conversion; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy`, `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle`, and `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_generate_trigger` prove helper branches and engine scheduling. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-10 | Advertised: SHIFT + CLIP STOP + loop/grid button | Clear the clicked loop. | `loop_clear(coords)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler → `ControlOperation::ClearLoops` → `ApplicationModel::apply_script_operation` → composite removal when needed and `Backend::clear_loop`, followed by authoritative media/composition state clearing. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now clears one clicked loop with SHIFT + CLIP STOP; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies `loop_clear` selector conversion. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added clicked-loop clear assertion. |
| AK-11 | Advertised: RECORD (REC ARM) + loop/grid button | Record the clicked loop. | `loop_trigger(coords, LoopMode_Recording)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` loads the bundled script and asserts the row-specific loop mode; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies selector/mode conversion; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy`, `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle`, and `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_generate_trigger` prove helper branches and engine scheduling. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-12 | Advertised: RECORD + DRY + loop/grid button | Re-record dry into wet on the clicked loop. | `loop_trigger(coords, LoopMode_RecordingDryIntoWet)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` loads the bundled script and asserts the row-specific loop mode; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies selector/mode conversion; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy`, `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle`, and `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_generate_trigger` prove helper branches and engine scheduling. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-13 | Advertised: GRAB (MUTE) + loop/grid button | Retroactively grab into the clicked loop from the running buffer. | `loop_trigger_grab(coords)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → `loop_trigger_grab` → `ControlOperation::Grab` → `ApplicationModel::grab_targets` → `Backend::grab_loops` plus post-grab transitions; a nonzero sync-loop length is an explicit precondition. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` proves the bundled GRAB branch reaches the application precondition error with a zero-length fake sync loop; `src/rust/shoop_app/src/lib.rs::tests::grab_policy_covers_targeted_selection_solo_and_immediate_completion` proves successful backend grab semantics with valid setup. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added contextual GRAB routing/precondition assertion; successful engine path was already covered by `src/rust/shoop_app/src/lib.rs::tests::grab_policy_covers_targeted_selection_solo_and_immediate_completion`. |
| AK-14 | Advertised: SHIFT + GRAB + loop/grid button | Toggle the default empty-loop recording action between `record` and `grab`. | `get_default_recording_action`; `set_default_recording_action` | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` presses SHIFT + GRAB + grid and asserts `DefaultRecordingAction::Grab`; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies string getter/setter shapes. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-15 | Advertised: SELECT + loop/grid button | Toggle selection of the clicked loop. | `loop_toggle_selected(coords)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SELECT + grid selects the exact loop; `src/rust/shoop_app/src/lib.rs::tests::lua_control_batches_use_authoritative_application_and_backend_paths` proves authoritative selection propagation. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-16 | Advertised: SHIFT + SELECT + loop/grid button | Toggle targeting of the clicked loop while respecting the application's single-target semantics. | `loop_toggle_targeted(coords)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SHIFT + SELECT + grid targets the loop; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` proves first-match/single-target toggle semantics. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-17 | Advertised mapping plus body detail: blank 2/sync-loop button, alone and with loop modifiers | Route note `88` to `{-1, 0}` and apply all applicable loop-button actions to the sync loop. Explicitly audit body-only consequences such as SET N CYCLES + sync-loop yielding zero and composition attempts involving the sync loop. | `note_to_loop_coords`; `handle_loop_pressed` with sync coordinates; all reached helper/control calls | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `note_to_loop_coords(88)` yields `{-1,0}`; subsequent branches follow the same trigger/track/global/composition paths as regular coordinates, ending at the appropriate backend or application boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts default sync-loop recording, VOLUME/PAN sync-track control, DEVICE + sync-loop = 0, and sync-loop as a serial composition source; selector tests verify `-1`. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added N-cycles, track-control, and composition coverage for note 88. |
| AK-18 | Advertised: hold SOLO, then release | Toggle solo on press and toggle it back on release for momentary behavior. | `shoop_helpers.toggle_solo`; `get_solo`; `set_solo`; SOLO held/permanent state | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SOLO press true and release false. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-19 | Advertised: SHIFT + SOLO | Toggle solo permanently: release SOLO must not restore the previous toggle. | SOLO note handlers; `toggle_solo`; permanent-state tracking | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SHIFT + SOLO remains true after SOLO and SHIFT release. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-20 | Advertised: hold SYNC, then release | Toggle synchronization active on press and toggle it back on release for momentary behavior. | `shoop_helpers.toggle_sync_active`; `get_sync_active`; `set_sync_active`; SYNC held/permanent state | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SYNC press toggles true and release restores false. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-21 | Advertised: SHIFT + SYNC | Toggle synchronization active permanently: release SYNC must not restore the previous toggle. | SYNC note handlers; `toggle_sync_active`; permanent-state tracking | `OK` — the getter/setter path is correct; the used `STATE_sync_active_toggle_permanent` is an undeclared Lua global rather than the declared local, but press assigns it before release and the contextual test proves behavior. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SHIFT + SYNC remains enabled after releases; no functional failure is observed. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-22 | Advertised: STOP ALL CLIPS | Stop every loop. | `loop_get_all`; `loop_trigger(..., LoopMode_Stopped)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts STOP ALL leaves every loop stopped; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies `loop_get_all` ordering and trigger conversion. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-23 | Advertised: SELECT + STOP ALL CLIPS | Deselect every loop. | `loop_select({}, true)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SELECT + STOP ALL deselects every loop. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-24 | Advertised: SHIFT + STOP ALL CLIPS | Clear every loop. | `loop_clear_all()` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler → `ControlOperation::ClearLoops` → `ApplicationModel::apply_script_operation` → composite removal when needed and `Backend::clear_loop`, followed by authoritative media/composition state clearing. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts SHIFT + STOP ALL leaves every loop empty and exercises composite cleanup. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-25 | Advertised: hold DEVICE/SET N CYCLES + grid button | Set apply-N-cycles to row-major values `1..63`, with the bottom-right grid button setting `0`. | grid coordinate conversion; `(x + y * 8 + 1) % 64`; `set_apply_n_cycles` | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts top-left = 1, bottom-right = 0, and note-88 body-only zero; the row-major arithmetic covers 1–63. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added sync-loop zero alongside existing endpoint assertions. |
| AK-26 | Advertised: fader without VOLUME or PAN | Moving a fader has no effect unless one of the two mode buttons is held. | `handle_cc` state guards | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `handle_cc` exits without a control call when both mode states are false, so no operation or backend boundary is reached. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now injects CC 48 without a mode and asserts gain remains 0 dB. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added inert-fader assertion. |
| AK-27 | Advertised: hold VOLUME + move regular-track fader | Map MIDI `0..127` to gain-fader `0.0..1.0` and set output gain for the intended regular track. | `cc_to_fader_track`; `track_set_gain_fader(track, value / 127.0)` | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts CC 48 value 127 under VOLUME produces +20 dB on regular track 0; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies fader-to-dB conversion. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-28 | Advertised: hold PAN + move regular-track fader | Map MIDI `0..127` to balance approximately `-1.0..1.0`, set the intended track, and verify the advertised stereo-only expectation. | `cc_to_fader_track`; `track_set_balance(track, value / 63.5 - 1.0)` | `MISMATCH` — `track_set_balance` exists, clamps to [-1,1], and accepts the call, but neither the script nor application checks `output_stereo`; mono tracks are changed despite the header. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now constructs regular track 0 as mono and asserts PAN still changes its balance to 1.0. | SUSPICIOUS — the advertised stereo-only restriction is not implemented. | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; it characterizes the mono-track PAN mismatch without changing production behavior. |
| AK-29 | Advertised: rightmost fader reserved for sync-loop track | CC `56` controls sync track `-1` for both VOLUME/gain and PAN/balance modes. | `cc_to_fader_track(56)`; `track_set_gain_fader(-1, ...)`; `track_set_balance(-1, ...)` | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now asserts CC 56 maps to sync track gain +20 dB and balance -1.0. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added both rightmost-fader modes. |
| AK-30 | Advertised: VOLUME + loop button | Toggle output mute on the track containing the clicked loop, including the sync loop when its loop button is used. | `shoop_helpers.track_toggle_muted(coords[1])`; `track_get_muted`; `track_set_muted` | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts regular-track mute and now asserts VOLUME + note 88 mutes the sync track. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added sync-track mute coverage. |
| AK-31 | Advertised: PAN + regular grid button | Toggle input mute once for the track represented by the grid column; unmuting must honor auto-mute-other-inputs. | `shoop_helpers.track_toggle_input_muted(coords[1], true)`; `track_get_input_muted`; `track_set_input_muted` | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts regular input unmute and now enables auto-mute, unmutes column 1, and asserts column 0 is muted; `src/rust/shoop_app/src/lib.rs::tests::lua_respecting_input_unmute_applies_global_policy_through_application` proves application/backend policy. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added contextual auto-mute-other-inputs assertion. |
| AK-32 | Advertised: PAN + sync-loop button | Toggle sync-track input mute and honor auto-mute-other-inputs when unmuting. | sync coordinate `-1`; `track_toggle_input_muted(-1, true)` | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now asserts PAN + note 88 enables sync-track input monitoring. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added sync input-mute coverage. |
| AK-33 | Advertised composition: hold SHIFT + DRY, click target | Enter composition mode and choose the first clicked loop as target without clearing an existing composition. | modifier state; `STATE_composition_active`; target state; no operation on first target click | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → `loop_compose_add_to_end` → `ControlOperation::ComposeAddToEnd` → cycle/source validation in `ApplicationModel::apply_script_operation` → `Backend::create/configure_composite_loop`; model schedule and backend composite are both updated. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` holds SHIFT + DRY and proves the first grid press only chooses a target; existing target contents are retained by append semantics; `src/rust/shoop_app/src/lib.rs::tests::script_composition_append_and_parallel_execute_on_engine_backend` reaches the engine backend. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; re-enters composition on an existing target and proves prior parallel sources remain before a new serial source. |
| AK-34 | Advertised composition: subsequent source loops pressed serially | Append each subsequently clicked source as a new serial section at the end of the target composition. | `loop_compose_add_to_end(target, source, false)`; press/release parallel counter | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → `loop_compose_add_to_end` → `ControlOperation::ComposeAddToEnd` → cycle/source validation in `ApplicationModel::apply_script_operation` → `Backend::create/configure_composite_loop`; model schedule and backend composite are both updated. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts separately released sources become ordered serial sections; `src/rust/shoop_app/src/lib.rs::tests::script_composition_append_and_parallel_execute_on_engine_backend` proves engine execution. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-35 | Advertised composition: multiple source loops held together | Put additionally pressed source loops in parallel with the current composition section while preserving source order and target contents. | `loop_compose_add_to_end(target, source, true)`; parallel counter | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → `loop_compose_add_to_end` → `ControlOperation::ComposeAddToEnd` → cycle/source validation in `ApplicationModel::apply_script_operation` → `Backend::create/configure_composite_loop`; model schedule and backend composite are both updated. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` asserts simultaneously held sources share one parallel section; `src/rust/shoop_app/src/lib.rs::tests::script_composition_append_and_parallel_execute_on_engine_backend` proves backend composite configuration and playback. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-36 | Advertised composition: SHIFT + DRY held throughout | Source releases update parallel state safely, and releasing DRY exits composition and clears the target so later loop presses are normal actions. | `handle_loop_released`; DRY note-off state reset | `OK` — release handling clamps the parallel count and DRY note-off clears active/target state; subsequent presses take normal branches. | `handle_loop_released` is script-local state only; after DRY release, later input follows the normal operation paths. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` releases sources and DRY, then performs later normal loop actions with no script error. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| AK-37 | Body-only: SHIFT + DEVICE/SET N CYCLES | Perform the controller LED reset/debug action instead of entering N-cycles mode. | DEVICE note-on branch; `reset`; global/loop LED resend | `OK` — subscriptions, timers, regex compilation, MIDI byte validation, endpoint-direction inversion, rate limiting, and callback payloads are implemented in `install_subscriptions` and `ScriptCallbacks::advance_midi`. | `ApplicationModel` publishes committed loop/global events → `ScriptManager` callback dispatch → bundled LED/cache logic → validated output queue → `MidiControlService::send`; MIDI output is the terminal boundary. | `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now sets apply-N-cycles to 7, presses SHIFT + DEVICE, asserts it remains 7, and observes a full reset burst. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` — PASS; added debug-reset/no-N-cycles assertion. |
| KB-01 | Body-only infrastructure: keyboard callback registration and event lifecycle | The bundled script receives translated non-repeat press/release events outside text entry, including synthetic releases on focus loss, with matching key/modifier constants. | `register_keyboard_event_cb(handle_keyboard)`; `KeyEventType_*`; `Key_*`; `KeyModifier_*` | `OK` — key/modifier/event constants are installed by `install_constants`; callback registration and payload dispatch are implemented by `install_subscriptions` and `ScriptCallbacks::dispatch_key_event`. | `shoop_egui::key_input::translate_events` → `AppIntent::KeyEvent` → `ApplicationModel::handle_script_key_event` → `ScriptManager`/`ScriptCallbacks::dispatch_key_event` → bundled callback. | `src/rust/shoop_egui/src/key_input.rs::tests::translates_script_values_modifiers_and_suppresses_repeats` and `src/rust/shoop_egui/src/key_input.rs::tests::text_entry_suppresses_new_keys_and_focus_loss_releases_held_keys` proves values, modifiers, repeat/text suppression, and focus-loss release; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` and `src/rust/shoop_scripting/src/lib.rs::tests::callbacks_expose_complete_payloads_and_are_non_reentrant` prove application-to-bundled callback dispatch. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-02 | Advertised arrows with empty selection; body-only fallback | Select `{0, 0}` when available; if no regular track exists, fall back to sync loop `{-1, 0}` rather than leaving selection empty. | `loop_get_which_selected`; `loop_select`; follow-up selection query | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts regular origin selection; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_selects_sync_fallback_and_characterizes_missing_ctrl_toggle` starts with only sync and asserts `{-1,0}` is selected. | PASS — expected and sufficiently tested | Added `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_selects_sync_fallback_and_characterizes_missing_ctrl_toggle` — PASS; covers the sync-only fallback. |
| KB-03 | Advertised: unmodified arrow key with selected loop(s) | Move the entire selected group one cell in the direction only when every destination exists and remains in bounds. | `shoop_helpers.move_selection`; `shoop_coords.move`; `loop_count`; `loop_select` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` moves selection between regular tracks, retries movement at the boundary, and asserts the selection remains in bounds; helper coordinate semantics execute in that production path. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added a movement attempt at the outer track boundary. |
| KB-04 | Advertised: CTRL + arrow | Expand selection in the requested direction, including edge/out-of-bounds selector behavior. | modifier mask; `shoop_helpers.expand_selection`; `shoop_coords.move`; `loop_select` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` sends CTRL + Left and asserts both adjacent loops are selected; `src/rust/shoop_egui/src/key_input.rs::tests::translates_script_values_modifiers_and_suppresses_repeats` and `src/rust/shoop_egui/src/key_input.rs::tests::text_entry_suppresses_new_keys_and_focus_loss_releases_held_keys` proves modifier bit translation. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-05 | Body-only: ALT + arrow | Shrink selection by removing the extreme row/column from the indicated direction. | modifier mask; `shoop_helpers.shrink_selection`; `shoop_coords.extreme`; `loop_select` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` sends ALT + Right and asserts one extreme column is removed; this is body-only contextual coverage. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-06 | Advertised: ESCAPE | Clear all loop selection. | `loop_select({}, true)` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` sends Escape multiple times and asserts all selection is cleared. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-07 | Advertised: SPACE | Perform the complete default action on selected loops: cancel queued transitions, obey record-versus-grab policy for empty loops, transition recording→playing, stopped nonempty→playing, and other states→stopped. | `loop_get_which_selected`; `shoop_helpers.default_loop_action`; its getter/trigger/grab chain | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts Space records an empty selected loop; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` asserts stopped-nonempty playback, playing-to-stop, queued-transition cancellation, and grab policy; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle` proves recording then playing on sync-cycle engine boundaries. | PASS — expected and sufficiently tested | Added `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` — PASS; covers the default-action branches not reached by the previous production test. |
| KB-08 | Advertised: R | With selection, trigger Recording; without selection, replace selection with all currently Recording loops. | `handle_loop_action(LoopMode_Recording)`; `loop_get_which_selected`; `loop_trigger`; `loop_get_by_mode`; `loop_select` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts both selected-loop transition to Recording and no-selection selection of loops already in that mode. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added explicit no-selection `R` branch coverage. |
| KB-09 | Advertised: P | With selection, trigger Playing; without selection, replace selection with all currently Playing loops. | `handle_loop_action(LoopMode_Playing)` and shared query/trigger/select calls | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts both selected-loop transition to Playing and no-selection selection of loops already in that mode. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added explicit no-selection `P` branch coverage. |
| KB-10 | Advertised: S | With selection, trigger Stopped; without selection, stop all loops rather than selecting stopped loops. | `handle_loop_action(LoopMode_Stopped)`; `loop_get_all`; `loop_trigger` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts selected loops stop and, after Escape, S stops every loop. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-11 | Advertised: L | With selection, trigger Playing Dry Through Wet; without selection, select all loops currently in that mode. | `handle_loop_action(LoopMode_PlayingDryThroughWet)` and shared calls | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts both selected-loop transition to Playing Dry Through Wet and no-selection selection of loops already in that mode. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added explicit no-selection `L` branch coverage. |
| KB-12 | Advertised: M | With selection, trigger Recording Dry Into Wet; without selection, select all loops currently in that mode. | `handle_loop_action(LoopMode_RecordingDryIntoWet)` and shared calls | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts both selected-loop transition to Recording Dry Into Wet and no-selection selection of loops already in that mode. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added explicit no-selection `M` branch coverage. |
| KB-13 | Advertised: I | Deduplicate tracks containing selected loops, toggle their input mutes as one group, and honor auto-mute-other-inputs when unmuting. | `toggle_selected_track_inputs`; `shoop_helpers.track_toggle_input_muted(tracks, true)`; track get/set input-muted APIs | `OK` — listed track getters/setters are installed by `install_track_api`; integer and integer-list selectors resolve through `parse_track_selector`; setters shadow state and enqueue typed track operations. | `ScriptCallbacks` dispatch → helper/direct track API → typed track `ControlOperation` → `ApplicationModel::apply_script_operation` → `apply_script_track_action`/`handle_track_input_monitoring` → `Backend::set_track_control`. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts I unmutes the selected track, then with auto-mute enabled unmutes the next selected track and mutes the first; `src/rust/shoop_app/src/lib.rs::tests::lua_respecting_input_unmute_applies_global_policy_through_application` proves backend policy. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added bundled I and auto-mute assertions. |
| KB-14 | Advertised: N, “Record next” | Choose the first empty loop in the one selected track, or recording track(s) when the selection rule does not yield one track; stop recording loops and record the chosen loop(s). | `shoop_helpers.record_into_first_empty(false)`; selected/recording/track/mode/length queries; `loop_trigger` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts N records the first empty loop for one selected track and now clears selection to assert fallback to the recording track. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added no-selection/recording-track fallback. |
| KB-15 | Advertised: G, “Grab” | Grab running-buffer data into selected loops retroactively, with empty-selection behavior explicitly characterized. | `loop_get_which_selected`; `loop_trigger_grab` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → `loop_trigger_grab` → `ControlOperation::Grab` → `ApplicationModel::grab_targets` → `Backend::grab_loops` plus post-grab transitions; a nonzero sync-loop length is an explicit precondition. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now asserts G reaches the documented application precondition error under the fake zero-length sync setup; `src/rust/shoop_app/src/lib.rs::tests::grab_policy_covers_targeted_selection_solo_and_immediate_completion` proves successful engine/backend grab behavior with a valid sync loop. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added bundled G routing/precondition assertion; successful path remains covered by `src/rust/shoop_app/src/lib.rs::tests::grab_policy_covers_targeted_selection_solo_and_immediate_completion`. |
| KB-16 | Advertised: O, “Overdub” | Choose first empty loop(s) by the same track rule as N, transition current recording loops to Playing, and record the chosen loop(s). | `shoop_helpers.record_into_first_empty(true)` and its query/trigger chain | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::Trigger` → `ApplicationModel::apply_script_operation` → `script_trigger_loops` → `Backend::transition_loop`/`transition_composite_loop`; global sync, apply-N-cycles, solo, and play-after-record are applied there. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts O transitions the current recording loop to Playing and records the next empty loop for both selected and no-selection recording-track paths. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added no-selection/recording-track overdub fallback. |
| KB-17 | Advertised: T | Choose one selected loop under the API's defined ordering, toggle it as the sole target, and untarget it if it is already targeted. | `loop_get_which_selected`; `loop_toggle_targeted` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts T targets one selected loop and U removes it; `src/rust/shoop_scripting/src/lib.rs::tests::every_control_function_is_invoked_with_retained_shapes_and_selectors` verifies first snapshot-order match and toggle-to-none semantics. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-18 | Advertised: U | Untarget every loop. | `loop_untarget_all()` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler/helper → `ControlOperation::SetLoopSelection`/`SetTarget` → `ApplicationModel::apply_script_operation`; authoritative application selection/target state is the boundary (selection additionally refreshes selected media). | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts U leaves every loop untargeted. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-19 | Advertised: W | Record selected loop(s) in synchronization with the currently targeted loop, including empty-selection/no-target behavior. | `loop_get_which_selected`; `loop_record_with_targeted` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `dispatch_key_event` → `loop_record_with_targeted` → `ControlOperation::RecordWithTargeted` → application target lookup → `Backend::set_loop_sync_source` and Recording transition. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` targets a loop, moves selection, presses W, and asserts the selected loop records; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` asserts both selected and empty no-target errors; `src/rust/shoop_app/src/lib.rs::tests::target_delay_is_derived_from_target_and_sync_lengths` covers target timing semantics. | PASS — expected and sufficiently tested | Added `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` — PASS; characterizes selected and empty W with no target while the existing production test covers success. |
| KB-20 | Advertised: C | Clear selected loops and do nothing outside that selector. | `loop_get_which_selected`; `loop_clear` | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks` dispatch → bundled handler → `ControlOperation::ClearLoops` → `ApplicationModel::apply_script_operation` → composite removal when needed and `Backend::clear_loop`, followed by authoritative media/composition state clearing. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` presses C and asserts the selected loop is empty. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |
| KB-21 | Advertised: `.` press and release | On press, immediately record empty stopped loops or play nonempty stopped loops without sync; on release/focus loss, immediately stop all sampler-started loops and restore repeat-sync. | `shoop_helpers.start_sampler`; `stop_sampler`; mode/length getters; `loop_set_repeat_sync`; `loop_transition` with both `Loop_Dont*` constants | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. | `ScriptCallbacks::dispatch_key_event` → bundled handler/helper → `ControlOperation::Transition`/`SetRepeatSync` → `ApplicationModel::apply_script_operation` → `Backend::transition_loop_aligned` and `Backend::set_loop_sync_source`. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` asserts empty-loop Period press/release; `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` asserts nonempty immediate play/release; `src/rust/shoop_egui/src/key_input.rs::tests::text_entry_suppresses_new_keys_and_focus_loss_releases_held_keys` proves focus-loss release; `src/rust/shoop_app/src/lib.rs::tests::latest_global_or_script_repeat_sync_policy_applies_to_existing_and_new_loops` proves repeat-sync backend policy. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` — PASS; added nonempty sampler play/release while existing tests cover empty recording and focus loss. |
| KB-22 | Advertised: one `0`–`9` key | Set apply-N-cycles to that digit, including `0` disabling bounded actions. | `as_number_key`; pressed-number state; `set_apply_n_cycles` | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` presses single digits (as part of the sequence) and asserts the resulting apply-N-cycles value; `src/rust/shoop_scripting/src/lib.rs::tests::complete_control_surface_is_installed_with_stable_constants` verifies key values. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added the intermediate single-digit value assertion. |
| KB-23 | Advertised combination: hold multiple `0`–`9` keys, e.g. `1` then `2` | Build an ordered decimal number such as `12`, avoid duplicate held instances, and remove released digits from held state without changing the already applied value until another press. | `handle_number_pressed`; `handle_number_released`; `update_n_cycles`; `set_apply_n_cycles` | `OK` — listed getter/setter pairs are installed by `install_global_api`, update the invocation snapshot, and enqueue typed global operations. | `ScriptCallbacks` dispatch → helper getter/setter → typed global `ControlOperation` → `ApplicationModel::apply_script_operation` → `handle_global_action`; authoritative global state and any repeat-sync/backend policy update are the boundary. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` holds 1 then 2, asserts 12, releases both, and continues without stale held digits. | PASS — expected and sufficiently tested | Extended `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` — PASS; added a post-release digit press proving held-number state was cleared. |
| KB-24 | Advertised cross-cutting modifier: hold CTRL | Momentarily invert global synchronization active while CTRL is held and restore it on release, while remaining compatible with CTRL + arrow expansion. The script body has no obvious standalone modifier handler, so trace host behavior before judging. | No direct call visible in `keyboard.lua`; inspect key translation/dispatch and any host global-sync handling | `MISSING` — `keyboard.lua` has no `Key_Control` press/release branch or sync getter/setter call, and egui key translation exposes modifiers only on other key events rather than a standalone CTRL event. | `key_input::translate_events` has no standalone modifier event → no `AppIntent::KeyEvent` for CTRL → bundled callback cannot toggle sync; direct `Key_Control` dispatch is ignored, so the path stops before a global operation. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_selects_sync_fallback_and_characterizes_missing_ctrl_toggle` directly dispatches `Key_Control` and characterizes sync remaining false; `src/rust/shoop_egui/src/key_input.rs::tests::translates_script_values_modifiers_and_suppresses_repeats` and `src/rust/shoop_egui/src/key_input.rs::tests::text_entry_suppresses_new_keys_and_focus_loss_releases_held_keys` confirms only mapped non-modifier keys are emitted. | SUSPICIOUS — the advertised momentary CTRL synchronization toggle is absent. | Added `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_selects_sync_fallback_and_characterizes_missing_ctrl_toggle` — PASS as a characterization test; it explicitly proves the advertised toggle is currently missing. |
| KB-25 | Advertised cross-cutting transition rule | Every applicable loop-transitioning binding is immediate or sync-scheduled according to global synchronization active state, with apply-N-cycles and targeted/sampler exceptions handled as documented. | `default_loop_action`; `record_into_first_empty`; `loop_trigger`; `loop_trigger_grab`; `loop_record_with_targeted`; `loop_transition`; global snapshot fields | `OK` — every listed helper exists in `shoop_helpers.lua`; every listed loop symbol/constant is installed by `install_loop_queries`, `install_loop_mutations`, and `install_constants`; single coordinates and coordinate lists are accepted by `parse_loop_selector` and resolve in snapshot order. Global sync/apply-N-cycles fields are present in `ControlSnapshot`. | `loop_trigger` operations reach `script_trigger_loops`, which derives delay from global sync and schedules bounded recording completion; targeted recording sets a sync source; sampler uses explicit no-wait/no-align transitions and repeat-sync overrides. | `src/rust/shoop_app/src/lib.rs::tests::production_keyboard_plays_manual_recording_on_next_sync_cycle` proves bundled Space scheduling on the dummy engine; `src/rust/shoop_app/src/lib.rs::tests::target_delay_is_derived_from_target_and_sync_lengths`, `src/rust/shoop_app/src/lib.rs::tests::latest_global_or_script_repeat_sync_policy_applies_to_existing_and_new_loops`, `src/rust/shoop_app/src/lib.rs::tests::grab_policy_covers_targeted_selection_solo_and_immediate_completion`, `src/rust/shoop_engine/tests/basic_loop.rs::basic_loop_planned_transitions_cancellation`, and `src/rust/shoop_engine/tests/audio_midi_loop_audio.rs::audio_record_dry_into_wet` cover target, sampler/repeat, cancellation, grab, and dry-mode exceptions. | PASS — expected and sufficiently tested | Not needed — the cited contextual and lower-level evidence is sufficient. |

## Inventory completion record

The leading documentation blocks were compared line by line with the table, then the complete bodies were reviewed through `handle_noteOn`, `handle_noteOff`, `handle_cc`, `handle_loop_pressed`, `handle_loop_released`, `handle_keyboard`, all registration callbacks, and every function in `shoop_helpers.lua` and `shoop_coords.lua` reached by those handlers. The result is 37 Akai rows and 25 keyboard rows. The body-only inventory includes held-button LED feedback, SHIFT + DEVICE reset, sync-loop modifier consequences, ALT + arrow shrinking, sync-only origin fallback, and keyboard event lifecycle behavior. No advertised binding or modifier combination remains outside the table.

## Ordered execution and verification

### Inventory and shared-path pass

First, compare the header comments and every event-handler branch against the master table. Append only genuinely missing significant body behaviors. Build a shared call map for helpers and APIs so repeated rows use consistent coordinate, selector, and operation semantics. Verify this pass by recording the compared header sections and body handlers in an audit note beneath the table; do not mark any behavioral verdict yet unless its full path has been read.

### Akai static audit pass

Resolve `AK-01` through `AK-37` in order. Trace MIDI endpoint ownership and direction before button behavior, then message routing/state precedence, Lua calls, application operations, backend effects, and LED output. Revisit all rows affected by shared state or modifier precedence. Verify the pass by ensuring every Akai row has complete API/path evidence and no unsupported inference from the broad APC test.

### Keyboard static audit pass

Resolve `KB-01` through `KB-25` in order. Start at egui key translation and release behavior, then trace the bundled callback, helpers, API conversion, application operation handling, and backend scheduling. Treat the advertised CTRL momentary-sync behavior as a required contract even if no implementation is found. Verify the pass by ensuring every keyboard row has complete API/path evidence and by rechecking no-selection and modifier cases separately.

### Existing-test evidence pass

Search Rust unit and integration tests by script constant, callback/API symbol, `ControlOperation`, application handler, backend method, and expected mode/state. Read setup and assertions before citing a test. For each row, record full, partial, or absent situational coverage. Verify that no row is called sufficiently tested solely because a lower-level API exists or a large integration test happens to load the script.

### Gap-test pass

For every row that lacks sufficient contextual evidence, add or extend focused Rust tests at the lowest layer that can still prove the script's real situation. Prefer loading `KEYBOARD_SCRIPT` or `AKAI_APC_MINI_MK1_SCRIPT`, injecting synthetic key/MIDI events, and asserting authoritative application plus backend/engine results. Use fake MIDI for deterministic controller input/output and a dummy engine backend where timing or engine transition semantics must be proved. Keep tests narrow enough that table rows can cite distinct assertions. Run targeted tests while iterating and record `PASS` or the exact expected `FAIL`; do not repair production behavior in this task.

### Final end-to-end validation

Perform a final header/body-to-table comparison, re-read every shared helper/API path whose conclusion changed, and ensure all acceptance criteria hold. Then record the exact outcomes of:

```sh
cargo fmt --all -- --check
RUSTFLAGS="-D warnings" cargo build --workspace
SHOOP_ALLOW_MISSING_BACKENDS=1 cargo test --workspace --features shoop_engine/app_backend -- --test-threads=1
```

Also run the focused test filters used during development and record them near the affected table rows. A failing newly added regression test is allowed, but the final summary must identify its row, command, failure, suspected broken layer, and why the test was retained. No unresolved table placeholder may remain.

## Suspicious-finding remediation goals

The three characterization tests must be converted to regression tests that assert the advertised behavior, and production code must be corrected until those assertions pass.

| Audit row | Remediation goal | Regression evidence required | Status |
|---|---|---|---|
| `AK-04` | Rebuild reset output from authoritative loop state and preserve correct cached colors across reconnects. | Initial/reset and reconnect assertions expect active-loop colors, not the defective off state. | PLANNED |
| `AK-28` | Make output PAN a no-op for mono tracks while retaining normal stereo balance control. | The APC integration test expects unchanged mono balance and changed stereo balance. | PLANNED |
| `KB-24` | Emit standalone CTRL press/release events and momentarily toggle synchronization without breaking CTRL + arrow expansion. | Input translation and bundled-keyboard tests expect sync inversion while held and restoration on release. | PLANNED |

The remediation is complete only when all three rows are promoted to `PASS`, the characterization wording is removed, focused regression tests pass, and the full validation gates pass.

## Final audit summary

- **PASS — expected and sufficiently tested: 59 rows.** All rows except `AK-04`, `AK-28`, and `KB-24` have sound static paths and combined contextual/lower-level Rust evidence reaching the relevant application, MIDI, backend, or engine boundary.
- **WORKS-BUT-UNTESTED: 0 rows.** Gaps found during the audit were covered by extending the production-script integration tests or adding focused characterization tests.
- **SUSPICIOUS: 3 rows.** `AK-04` loses authoritative LED state on initial/reset paths and contains an incorrect cache-row check; `AK-28` changes balance on mono tracks despite the advertised stereo-only restriction; `KB-24` has no implementation or input event path for the advertised momentary CTRL synchronization toggle.
- **NOT-APPLICABLE: 0 rows.** MIDI/LED and script-local state rows use their actual non-engine terminal boundaries rather than being waived.

All Lua functions used by implemented bindings exist and accept the supplied shapes. The notable implementation defects are the `AK-04` cache lookup, the missing stereo guard in `AK-28`, the missing CTRL behavior in `KB-24`, and the harmless-but-suspicious SYNC permanence variable name mismatch noted in `AK-21`. No other broken app-to-engine path was found.

Rust coverage added or extended in `src/rust/shoop_app/src/lib.rs`:

- `production_keyboard_script_handles_navigation_modes_numbers_targets_and_releases` now covers input mute/auto-mute, boundary-preserving movement, single/multi-digit state, no-selection mode lookup, G routing, and selected/recording-track N/O fallback.
- `production_keyboard_selects_sync_fallback_and_characterizes_missing_ctrl_toggle` covers the sync-only arrow fallback and explicitly characterizes missing CTRL behavior.
- `production_keyboard_default_action_covers_stopped_cancel_and_grab_policy` covers all significant default-action branches, nonempty sampler behavior, and W no-target behavior.
- `unchanged_apc_script_drives_authoritative_state_and_bounded_led_output` now covers held/global LEDs, debug reset, dry playback, clicked clear, grab routing, sync-loop N-cycles/track/composition behavior, preserving an existing composition, inert faders, mono PAN, sync fader/mutes, and input auto-mute.
- `production_apc_led_colors_and_reconnect_reset_characterize_cached_color_bug` covers all four loop LED colors and explicitly characterizes the reconnect cache defect.

All new and extended tests pass; no failing Rust test was retained. The suspicious rows are represented by passing characterization assertions that state current behavior and preserve evidence until production fixes are approved.

Prioritized follow-up work:

1. Fix `get_loop_color` and initialize/reset LEDs from authoritative loop state, then invert the `AK-04` characterization into a regression assertion for preserved color.
2. Decide whether PAN should truly be stereo-only; either enforce the guard and update `AK-28`, or correct the advertised contract.
3. Define a realizable standalone CTRL press/release input contract and implement the momentary sync toggle for `KB-24`, while preserving CTRL + arrow expansion.
4. Rename/localize the SYNC permanence state variable in the Akai script.

## Validation record

All commands below were run from the repository root after the audit and tests were completed:

- `cargo fmt --all -- --check` — **PASS**.
- `RUSTFLAGS="-D warnings" cargo build --workspace` — **PASS**.
- `SHOOP_ALLOW_MISSING_BACKENDS=1 cargo test -p shoop_app production_keyboard -- --nocapture` — **PASS**, 5 tests.
- `SHOOP_ALLOW_MISSING_BACKENDS=1 cargo test -p shoop_app apc_script -- --nocapture` — **PASS**, 2 tests; the native virtual-MIDI test reported an environment skip because ALSA sequencer facilities were unavailable, while the deterministic fake-MIDI production-script test passed.
- `SHOOP_ALLOW_MISSING_BACKENDS=1 cargo test -p shoop_app production_apc_led_colors_and_reconnect_reset_characterize_cached_color_bug -- --nocapture` — **PASS**, 1 test.
- `SHOOP_ALLOW_MISSING_BACKENDS=1 cargo test --workspace --features shoop_engine/app_backend -- --test-threads=1` — **PASS**; all executed workspace unit, integration, and documentation tests passed with no failing or ignored tests. Host audio/MIDI discovery emitted expected missing-device diagnostics, but the command exited successfully.

The fake-MIDI tests, not the skipped native-host branch, provide deterministic Akai coverage used by the table.

## Execution contract

- Keep this document updated as work progresses and replace table cells immediately when evidence is established.
- Commit each completed script audit, test group, or other meaningful milestone.
- Investigation and test steps may be revised when new evidence warrants it.
- Design rules may be revised only for a documented, well-supported reason.
- Goals and acceptance criteria must not be changed without explicit user approval.
- If a path cannot be resolved after following its symbols and searching tests, stop that row with the evidence gathered, attempted paths, exact blocker, and next input needed; do not guess.
